- name: "Install Utilities"
  apt:
    name:
      - git
      - gpg
      - htop
      - jq
      - tmux
      - openvpn
      - wireguard
    state: present
    update_cache: true

- name: "Install vims"
  apt:
    name:
      - neovim
      - vim-common
    state: present
    update_cache: true

